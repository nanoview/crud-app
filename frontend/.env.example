// For local development use the backend running on localhost
# For local development you can set this to http://localhost:5000/api/books
# In production (Vercel) set it to your deployed backend URL (Render):
VITE_API_URL=https://crud-app-9rm5.onrender.com/api/books
